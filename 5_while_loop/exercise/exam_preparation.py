number_of_fails = int(input())

fails = 0
sum_grade = 0
problems_count = 0
last_problem = ""

while name != "Enough":
    pass
    if grade <= 4:
        fails += 1

    else:
        problems_count += 1
        sum_grade += grade
        last_problem = name
    name = input()
    grade = float(input)
